# Minimal values used by tests/v5_2_migration_imports.rs to drive
# `helm lint` and `helm template` against charts/janus.
postgresql:
  externalUrl: postgres://janus:test@pg.test:5432/janus

secrets:
  jwtSecret: "test-jwt-secret-do-not-use-in-prod"
  encryptionKey: "test-encryption-key-do-not-use-in-prod"
  openaiApiKey: "sk-test"

ingress:
  enabled: true
  className: nginx
  hosts:
    - host: janus.test
      paths:
        - path: /
          pathType: Prefix

serviceMonitor:
  enabled: true

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
