Velox {{ .Chart.AppVersion }} is being deployed.

1. Wait for pods to become ready:

   kubectl --namespace {{ .Release.Namespace }} rollout status deployment/{{ include "velox.fullname" . }}

2. Hit the gateway:

{{- if .Values.ingress.enabled }}
   Configured hosts:
   {{- range .Values.ingress.hosts }}
     - https://{{ .host }}/v1/chat/completions
   {{- end }}
{{- else }}
   Local port-forward:

   kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "velox.fullname" . }} 8080:{{ .Values.service.port }}
   curl http://localhost:8080/health
{{- end }}

3. Admin dashboard + OpenAPI docs:

   /admin/docs              — Swagger UI
   /admin/openapi.json      — OpenAPI 3.1 spec

4. Bootstrap an admin user and an API key via the CLI:

   velox keys create --name "first-prod-key"

5. Operator playbooks:
   - HA setup:        docs/deployment/ha.md
   - Helm reference:  docs/deployment/helm.md

Need help? https://github.com/farzad/velox/issues
